{
  "version": 2,
  "buildCommand": "cd app && npm install",
  "outputDirectory": "app",
  "functions": {
    "app/api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/app/api/$1" },
    { "src": "/(.*)", "dest": "/app/$1" }
  ]
}
